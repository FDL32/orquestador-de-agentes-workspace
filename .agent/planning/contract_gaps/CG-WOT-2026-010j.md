# CONTRACT_GAP: WOT-2026-010j

## Ticket

- **ticket_id:** WOT-2026-010j
- **gap_type:** forbidden_surface_needed
- **reported_by:** Orchestrator
- **reported_at:** 2026-06-17

## Trigger

El ticket produjo correctamente su deliverable en `repo_motor`, pero el cierre
canonico queda bloqueado por un bug del motor en
`scripts/check_deliverables_exist.py`.

## Verified Evidence

- Deliverable real:
  `repo_motor/docs/test_performance/test_performance_baseline_WOT-2026-010j.md`
- Commit del reporte: `c05dbfe`
- Commit del fix de packet/FLT: `cb01f28`
- El gate de implementacion evidencia en `.agent/agent_controller.py` es
  no-bypassable por contrato.
- Duplicar el artefacto en `repo_destino` contradice la Decision
  Arquitectonica de `010j`.

## Why Builder Must Stop

Cerrar `010j` sin arreglar el gate obligaria a:

1. duplicar un artefacto entre `repo_motor` y `repo_destino`, o
2. relajar/manualizar un gate fail-closed.

Ambas opciones rompen el contrato que `010j` protege.

## Required Resolution

- Abrir y ejecutar `WOT-2026-010n`.
- Corregir `scripts/check_deliverables_exist.py` para resolver deliverables
  Builder segun namespace FLT y `delivery_authority`.
- Reintentar el cierre canonico de `010j` sin duplicar el reporte.

## Forbidden Shortcut

- No copiar el deliverable de `repo_motor` a `repo_destino` solo para satisfacer
  el gate.

## Resolved

- **resolved_at:** 2026-06-17
- **resolved_by:** WOT-2026-010n (commits `b355cb0`, `453967d`, `c7249b8` en
  repo_motor).
- **Evidencia:** ver `execution_log.md` seccion "Reintento del cierre real de
  `WOT-2026-010j`". El gate corregido resuelve el `work_plan.md` real que
  bloqueo el cierre (recuperado de `git show b264987:...`) sin `MISSING`
  espurios y sin duplicar el reporte en `repo_destino`.
- **Forbidden Shortcut respetado:** el deliverable sigue viviendo unicamente
  en `repo_motor/docs/test_performance/test_performance_baseline_WOT-2026-010j.md`.
