# CONTRACT_GAP -- WOT-2026-013r

## Estado
BLOCKED para cierre canonico. El trabajo del Paso 1 esta hecho y verificado
(8 patches repuntados al modulo importado + barrera de binding honesta), pero el
DoD binario principal (linea 102) NO es satisfacible en el Paso 1 por una razon
fisica del codigo, no por falta de esfuerzo. Se escala el Paso 2 (dedup de forks),
que el propio packet marca como "requiere reaprobacion humana explicita".

## El gap (verificado por experimento, 3 veces)
DoD linea 102 exige: "revertir el fix de los patches -> 
`python -m pytest tests/unit/test_upgrade.py -q` FALLA; con el fix -> pasa".

Hecho verificado en codigo:
- `scripts.upgrade.shutil IS scripts.upgrade_agent_system.shutil` -> **True**.
  Ambos modulos hacen `import shutil`, por lo que comparten EL MISMO objeto modulo
  `shutil` (cache de `sys.modules`).
- Consecuencia: `patch("scripts.upgrade.shutil.copytree")` y
  `patch("scripts.upgrade_agent_system.shutil.copytree")` parchean el MISMO
  atributo del MISMO objeto. Tras parchear uno, `up.shutil.copytree is ct` -> True.
- Experimento: parcheando el modulo "equivocado" (`scripts.upgrade.shutil`) el SUT
  sigue interceptando 8 copias (== len(CRITICAL_PATHS)). El repunte del target es
  FISICAMENTE INDISTINGUIBLE en runtime.

Por tanto: revertir SOLO el string de los 8 patches NO cambia el comportamiento de
la suite focal (sigue 18 passed). El DoD literal "revertir -> FALLA" no puede
cumplirse mientras los dos forks compartan el objeto `shutil`.

## Por que el Paso 1 no basta (la barrera honesta vs el DoD literal)
- La distincion REAL entre forks SI existe: definen clases `UpgradeManager`
  DISTINTAS (`scripts.upgrade.UpgradeManager is scripts.upgrade_agent_system.UpgradeManager`
  -> False). El SUT importado vive en `scripts.upgrade_agent_system`.
- Sobre eso construi una barrera honesta de BINDING (mutation-verified: mutar el
  assert `__module__` al fork viejo -> pytest FALLA). Mejora la higiene del target
  y protege el SUT.
- PERO el Manager mide contra el DoD literal (revertir patches -> fallo focal), que
  esa barrera no satisface. El Manager tiene razon: con el criterio escrito, esto
  es falso-verde. No reinterpreto el DoD unilateralmente.

## Conflicto irreducible dentro del Paso 1
- Si NO toco codigo productivo (Paso 1): el shutil compartido hace el repunte
  indistinguible -> DoD literal inalcanzable.
- La UNICA via para que "revertir patches -> FALLA" sea cierto es dar a cada fork
  un binding de `shutil` propio (p.ej. `from shutil import copytree` en uno) o
  deduplicar los forks. Eso es codigo productivo de upgrade = Paso 2, explicitamente
  fuera del Paso 1 sin reaprobacion (work_plan lineas 75-76, 90).

## Resolucion propuesta (decision humana)
1. RECOMENDADA: acometer **WOT-2026-013t** (Paso 2) con reaprobacion: deduplicar /
   dar binding independiente a `upgrade.py` vs `upgrade_agent_system.py`, alinear
   `README` (canonico declarado), y entonces el DoD literal de 013r se vuelve
   alcanzable (revertir target del patch -> fallo real). Mayor blast radius.
2. Enmienda de DoD: aceptar la barrera de binding como criterio de 013r (el Manager
   la sugiere como no-bloqueante) y reescribir la linea 102 ANTES de re-review, para
   que el Manager mida contra la barrera real. Mantiene Paso 1, sin tocar producto.
3. Cerrar 013r como parcial (higiene + barrera de binding entregadas) y mover el
   falso-verde-binario a 013t.

## Estado de entrega del Paso 1 (verificado)
- 8 patches -> `scripts.upgrade_agent_system.shutil.*` (modulo importado). wrong=0.
- Barrera `TestUpgradeMockTargetBarrier` (3 tests), mutation-verified.
- `pytest tests/unit/test_upgrade.py -q` -> 21 passed. ruff check/format OK.
- `validate --json --force` -> 0/0. Suite canonica al HEAD 8e84a25 -> exit 0.
- Commit motor: 8e84a25.

## Siguiente accion
PARA. No reescribo el DoD ni acometo el Paso 2 sin reaprobacion humana explicita.
Follow-up registrado: WOT-2026-013t en backlog.md (Paso 2 / dedup de forks).
