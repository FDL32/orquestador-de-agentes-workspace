# PLAN WT-2026-247a - Higiene de suite: aislamiento de tests y bugs de regex/mock/lock

## Metadata
- **ID:** WT-2026-247a
- **Title:** Higiene de suite: aislamiento de tests y bugs de regex/mock/lock
- **Scope:** system/testing-hygiene
- **Priority:** Media
- **Estado:** PENDING
- **deliverable_type:** code
- **Asignado a:** BUILDER
- **Depende de:** WT-2026-245c

## Objetivo tecnico
Recuperar la fiabilidad de 7 tests rotos de la suite separando dos familias de problema: contaminacion de estado real del workspace y bugs en la propia logica/expectativa de los tests. El ticket debe dejar esos tests verdes sin introducir mocks irreales, sin ocultar bugs reales del producto y sin relajar contratos del sistema para acomodar tests fragiles.

## Contexto verificado
- El backlog ya separa explicitamente dos grupos de fallos:
  - **Grupo A - Contaminacion de estado real (3 tests):** leen artefactos vivos del workspace o del runtime real y no aislan su entorno.
  - **Grupo B - Bugs en el propio test (4 tests):** regex legacy, mock drift, expectativas desalineadas o hipotesis dudosas sobre orden de guards.
- `WT-2026-246b` ya quedo cerrado canonicamente; este ticket es de higiene de suite y no debe reabrir closeout/launcher.
- La nota mas delicada del backlog es `test_launcher_does_not_write_pid`: antes de corregir el test, hay que verificar si el launcher actual sigue escribiendo `pid` por contrato o si el test esta detectando un bug real del producto.
- Este ticket no debe convertir la suite en "verde cosmetico": cualquier test arreglado debe seguir validando un observable real del sistema.

## Alcance
- Aislar con `tmp_path`, fixtures o datos sinteticos los 3 tests que hoy dependen de estado real del workspace.
- Actualizar el test del patron `WT-` al contrato canonico vigente, sin reintroducir compatibilidad legacy incorrecta.
- Corregir el mock drift del test de `motor_root_gates` para que pruebe la seam real que el codigo invoca.
- Revisar el orden de guards y/o la expectativa del test en `test_scope_gate.py::test_mark_ready_blocks_on_zero_overlap`.
- Determinar explicitamente si `test_launcher_does_not_write_pid` refleja bug real del launcher o expectativa incorrecta del test; solo entonces corregir codigo o test.

## Files Likely Touched
- `tests/test_builder_lock.py`
- `tests/test_review_packet_evidence_gate.py`
- `tests/test_motor_root_gates.py`
- `tests/unit/test_scope_gate.py`
- `scripts/launch_agent_terminals.ps1`
- `.agent/agent_controller.py`

## Read/inspect only
- `.agent/collaboration/backlog.md`
- `.agent/collaboration/STATE.md`
- `.agent/collaboration/TURN.md`
- `.agent/collaboration/execution_log.md`
- `.agent/runtime/events/events.jsonl`
- `PROJECT.md`
- `docs/KNOWN_FAILURE_PATTERNS.md`

## Secuencia de implementacion
1. Reproducir los 7 fallos exactos y agrupar evidencia por test.
2. Corregir primero el Grupo A garantizando aislamiento real de fixtures y evitando leer runtime vivo del workspace.
3. Para `test_lock_schema_contains_required_fields`, actualizar el patron al formato canonico usando `from bus.ticket_id import TICKET_ID_PATTERN` (o `TICKET_ID_RE`) en vez de duplicar el regex inline, y solo cambiar la expectativa si el contrato productivo vigente ya lo exige.
4. Para `test_launcher_does_not_write_pid`, inspeccionar launcher y contrato antes de decidir si el bug es del producto o del test.
5. Para `test_regression_cwd_project_root_breaks_check`, sustituir el mock drift por un patch sobre la seam real invocada por produccion.
6. Para `test_mark_ready_blocks_on_zero_overlap`, verificar si `_release_builder_lock` es comportamiento correcto del guard actual o si el orden esperado por el test quedo obsoleto.
7. Ejecutar quality gates focales y registrar evidencia exacta en `execution_log.md` cuando el ticket se active.

## Politica de commit
- El commit productivo de `WT-2026-247a` debe incluir solo superficies de suite/testing y codigo productivo minimo si un test demuestra bug real.
- No usar este ticket para refactors amplios de launcher, controller o bus fuera de lo estrictamente necesario para alinear el contrato observable.
- Si un test nuevo o helper adicional es imprescindible, anadirlo antes a `Files Likely Touched` y registrar justificacion CEM en `execution_log.md`.
- Si un test se "arregla" relajando la expectativa sin demostrar por que era incorrecta, el ticket debe bloquearse como falso-verde.

## Criterios binarios de aceptacion
- Los 7 tests identificados en backlog pasan.
- Los 3 tests del Grupo A dejan de depender de artefactos vivos del workspace real.
- `test_launcher_does_not_write_pid` queda resuelto con evidencia de contrato: o se corrige producto o se corrige expectativa, pero no queda ambiguo.
- No hay mock drift en `test_motor_root_gates.py`.
- No hay floor assertions ni asserts cosmeticos introducidos en esta pasada.
- `ruff check` y `validate --json --project-root <repo_destino>` pasan.
- Existe commit visible de `WT-2026-247a` en `repo_motor`.

## Riesgos conocidos
- Riesgo de "poner verde" la suite parcheando tests sin verificar el contrato real.
- Riesgo de convertir fixtures realistas en mocks esteriles que ya no cubran seams reales.
- Riesgo de tocar launcher/controller para contentar un test cuya expectativa era incorrecta.
- Riesgo de que el fallo del `pid` sea un bug real y se oculte cambiando solo el test.
- Riesgo de contaminar el ticket con follow-ups del launcher o del closeout ya cerrados en `246b`.

## Non-goals
- No reabrir `WT-2026-246b` ni mezclar deuda de closeout/authority.
- No hacer refactor general de toda la suite; solo los 7 fallos mapeados.
- No introducir compatibilidad legacy de prefijos de ticket si el contrato canonico ya es `WT-`.
- No cambiar guardias productivas solo para hacer pasar un test sin evidencia de bug real.

## Decision Arquitectonica

**Problema:** la suite combina dos clases de fallos muy distintas: tests que dependen de estado real del entorno y tests cuya propia logica esta desalineada con el contrato productivo actual. Mezclarlos como si fueran un unico tipo de fallo degrada la seÃƒÆ’Ã‚Â±al de la suite.

**Decision:** tratar `247a` como ticket de higiene de alta fidelidad: aislar el entorno de los tests contaminados y corregir solo aquellas expectativas/tests cuya desalineacion pueda demostrarse contra el comportamiento productivo real.

**Consecuencia operativa:** la suite recupera poder diagnostico sin convertir los tests en green cosmÃƒÆ’Ã‚Â©tico ni forzar cambios de producto dudosos.

## Quality Gates
```powershell
C:\Users\fdl\Proyectos_Python\orquestador_de_agentes\.venv\Scripts\python.exe -m pytest tests/test_builder_lock.py tests/test_review_packet_evidence_gate.py tests/test_motor_root_gates.py tests/unit/test_scope_gate.py -v
C:\Users\fdl\Proyectos_Python\orquestador_de_agentes\.venv\Scripts\python.exe -m ruff check tests/test_builder_lock.py tests/test_review_packet_evidence_gate.py tests/test_motor_root_gates.py tests/unit/test_scope_gate.py
C:\Users\fdl\Proyectos_Python\orquestador_de_agentes\.venv\Scripts\python.exe .agent\agent_controller.py --validate --json --project-root C:\Users\fdl\Proyectos_Python\orquestador_de_agentes_workspace
```

## Evidencia esperada
- Salida reproducible de los 7 fallos antes del fix.
- Diff revisable en los 4 archivos de tests, y solo en producto si un test demuestra bug real.
- Evidencia de FAIL sin fix / PASS con fix al menos para los casos donde el contrato sea binario y aislable.
- `ruff` verde sobre los tests tocados.
- `validate --json --project-root ...` con `errors` vacio y `warnings` vacio.