# PLAN WT-2026-224a - Supervisor relaunch guard

## Objetivo
Evitar que el supervisor relance un Builder nuevo cuando `_builder_alive()`
siga considerando vivo al Builder del round activo.

## Orden de trabajo recomendado
1. Leer `work_plan.md`, este plan y `AUDIT_WT-2026-224a.md`.
2. Confirmar en `bus/supervisor.py` el camino real de `_relaunch_builder()`.
3. Identificar el helper canonico de liveness que ya existe.
4. Escribir primero una prueba nueva de overlap con relaunch suprimido.
5. Implementar la barrera minima.
6. Anadir prueba del camino valido cuando `_builder_alive()` devuelve `False`.
7. Ejecutar tests focales y `ruff`.
8. Validar el `repo_destino` con `agent_controller.py --validate --json --project-root .`.

## Seams confirmados por auditoria
- `bus/supervisor.py:_relaunch_builder()` alrededor de L2022: punto real del
  relaunch.
- `bus/supervisor.py:_bootstrap_requeue_if_needed()` alrededor de L822-L939:
  ya reconoce `builder_lock_fresh` y lo trata como razon para no reencolar.
- `bus/supervisor.py:_builder_alive()` alrededor de L1605-L1650: helper
  canonico de liveness; usa `BUILDER_EXIT` posterior al lock y mtime del lock,
  no PID.
- `bus/supervisor.py:_verify_builder_start()` alrededor de L2294-L2387: ya usa
  `builder_lock` como verificacion post-spawn.
- `tests/test_relaunch_topology.py:test_relaunch_blocks_on_invalid_topology()`:
  cobertura base de entrada por `_relaunch_builder()`.

## Reglas de ejecucion
- No redisenar el protocolo entero.
- No introducir heartbeats persistentes nuevos en este ticket.
- No mezclar `WT-2026-221c`.
- No mezclar `WT-2026-223a`.
- El bloqueo debe ser binario y observable usando `_builder_alive()` como
  autoridad, sin introducir PID como criterio nuevo.

## Verificacion ejecutable minima
Desde el `repo_motor`:

```powershell
python -m pytest tests/test_relaunch_topology.py tests/test_launch_agent_terminals_script.py -q
python -m ruff check bus/supervisor.py tests/test_relaunch_topology.py tests/test_launch_agent_terminals_script.py
```

Nota: el test de overlap con supresion de relaunch es un deliverable nuevo de
este ticket; no basta con rerun de los tests de topologia ya existentes.

Desde el `repo_destino`:

```powershell
python ..\orquestador_de_agentes\.agent\agent_controller.py --validate --json --project-root .
```
