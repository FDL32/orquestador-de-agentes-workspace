# CONTRACT_GAP -- WOT-2026-012a (estrechado tras review del Manager)

## Estado
BLOCKED para handoff canonico. El trabajo productivo esta hecho y verificado; el
unico rojo restante del encoding guard son 3 control chars HEREDADOS de HEAD en el
historico/snapshot, cuya fuente investiga WOT-2026-011c.

## Correcciones aplicadas tras el review (los 2 MAJOR del Manager)
- MAJOR 1 (snapshot no byte-a-byte): RESUELTO. El snapshot se hizo con cp del
  working tree, que tenia BOM inyectado post-commit. Regenerado desde
  `git show HEAD:.agent/collaboration/backlog.md`. Ahora sha256 coincide EXACTO
  con HEAD: b1d3305143e6fc12c992982c923dedfcab2b359c34f4b6a50690588bbb039be8.
  Sin BOM (primeros bytes 23 20 = "# ").
- MAJOR 2 (sobre-atribucion + BOM en superficie propia): RESUELTO. `execution_log.md`
  esta en mi FLT; su BOM (inyectado en working tree; HEAD no lo tenia) era MI
  superficie, no 011c. BOM removido. `backlog_done.md` regenerado desde el snapshot
  sin BOM.

## Rojo restante del guard (acotado y verificado)
`python scripts/check_encoding_guard.py <entregables>` tras las correcciones ->
exit 1 SOLO por:
- 3 control chars en `_archive/backlog_pre_012a.md` y `_archive/backlog_done.md`:
  `\x07udit` (->audit), `\x0Balidate` (->validate), `\x08ui-self` (->bui-self).
YA NO aparecen: backlog.md vivo, execution_log.md, CG (todos limpios).

## Por que los control chars no son scope de 012a (verificado, no atribuido)
- `git show HEAD:.agent/collaboration/backlog.md` -> contiene los 3 control chars
  (`\x07 \x0B \x08`). Estan en la region historica del backlog committeado.
- El snapshot DEBE ser byte-fiel a HEAD (criterio del ticket + exigencia del review).
  Una copia fiel arrastra inevitablemente la corrupcion de fuente.
- Reconstruir las letras comidas (udit->audit) es adivinar contenido y enmascara la
  fuente; eso es el objetivo declarado de WOT-2026-011c (BOM/control-char SOURCE
  audit), explicitamente pending y separado.

## Conflicto estructural irreducible dentro de 012a
- Si el snapshot/historico es byte-fiel a HEAD -> arrastra los control chars -> guard rojo.
- Si limpio los control chars -> el snapshot deja de ser fiel (rompe el criterio que el
  propio Manager exigio) y reconstruir letras pisa el scope de 011c.
No hay salida dentro de 012a sin (a) resolver la fuente en 011c, o (b) excluir
`_archive/**` del guard (motor file, fuera de FLT/repo_destino), o (c) enmienda humana.

## Resolucion propuesta (decision Manager/humano)
1. RECOMENDADA: secuenciar **WOT-2026-011c** (identificar/limpiar la fuente
   BOM+control-char en `.agent/collaboration/`) antes de cerrar 012a. Resuelta la
   fuente, se regenera snapshot/historico limpios desde un HEAD ya saneado y el guard
   pasa sin que 012a reconstruya nada a ciegas.
2. Enmienda de scope: autorizar a 012a a reconstruir las 3 letras con evidencia de
   contexto (audit/validate/bui-self son inequivocas) y registrarlo como desviacion CEM.
   Mas rapido, pero mezcla 012a con el trabajo de 011c.
3. Excluir `_archive/**` del guard: ticket de motor aparte.

## Estado de entrega
- backlog.md vivo: limpio, 197 lineas, 13 filas, Reactivation OK, 0 terminales.
- snapshot: byte-fiel a HEAD (sha coincide).
- Sin commit ni handoff: el handoff solo existe si el gate pasa. Se reemite este CG
  estrechado para decision.
