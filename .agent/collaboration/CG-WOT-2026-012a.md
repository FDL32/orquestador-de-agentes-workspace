# CONTRACT_GAP -- WOT-2026-012a (deliverable_type mal clasificado)

## Estado
BLOCKED para handoff. TODO el trabajo productivo esta hecho, verificado y verde.
El bloqueo es estructural del contrato, no de la implementacion: T-012A-001 declara
`deliverable_type: mixed`, pero la entrega real es 100% documental en repo_destino.
El gate `commit_visible` (no bypassable por --force/--scope-override) exige un commit
de codigo en repo_motor que mencione el ticket, lo cual NO procede para 012a.

## Trabajo completado (verificado)
- backlog.md cola viva parseable (199 lineas), 0 terminales, 002c (completed-partial) permanece.
- 011j movido al historico (fila + ficha). ### WOT-2026-012a integra en vivo Y en historico.
- Reconstruccion acotada: SOLO los 3 ctrl chars verificados por 011c (\a->audit, \v->validate,
  \b->bui-self) en backlog_done.md y backlog_pre_012a.md. 0 strips ciegos, 0 chars extra.
- Encoding guard: exit 0 (4 superficies). Validate: 0/0.
- Suite canonica motor: 3032 passed, 20 skipped, 0 failed, level=all, tested_sha=fb2c604 (fresh_green).
- Commit deliverable repo_destino: 9640511 + 4656755 (evidencia de cierre).

## El gap (causa raiz, VERIFICADA)
El pre-handoff guard devuelve valid=False por UN solo subcheck:
- `commit_visible: no_visible_commit` -> "No commit in the last 20 repo_motor commits
  names WOT-2026-012a: a code/mixed packet must carry a visible productive commit."
- assert_ticket_commit_visible (pre_handoff_guard.py:614) SOLO exime
  documentation/research/analysis (`deliverable_type not in _SUITE_REQUIRED_TYPES`).
- 012a es `mixed` -> NO exento -> exige commit de codigo en motor.
- PERO el deliverable real (git show 9640511) toca SOLO .agent/collaboration/*.md del
  destino. 0 commits del motor mencionan 012a, y NO deben: 012a no toca codigo del motor.
- El propio guard lo dice: "If the delivery legitimately has no code commit, the ticket
  type should not be code/mixed."
- Otros gates ya estan OK: canonical_suite=fresh_green, dirty_tree=False,
  missing_checkpoint=False, forbidden_surface=[], scope=OK.

## Falso positivo adicional detectado (no bloquea via script, informativo)
- En invocacion como modulo (no como script), `from delivery_hygiene_check import
  check_archive_rename_complete` (pre_handoff_guard.py:933) falla por sys.path y marca
  valid=False con archive_rename_guard_error. Como SCRIPT directo (flujo real de
  mark-ready) el import resuelve y archive_rename_guard_error=None. No hay limbo real
  (git status _archive/plan_audit limpio). Mencionado para el Manager por si quiere
  endurecer el import a `from scripts.delivery_hygiene_check import ...`.

## Resolucion propuesta (decision Manager/humano)
1. RECOMENDADA: enmendar T-012A-001 `deliverable_type: mixed -> documentation`.
   012a es puramente documental/operativo en repo_destino; no produce ni debe producir
   commit de codigo en repo_motor. Con `documentation`, commit_visible y
   canonical_suite quedan exentos (ambos chequean _SUITE_REQUIRED_TYPES) y el handoff
   procede honestamente. Es exactamente lo que el guard recomienda.
   Tras la enmienda: re-validar (0/0), re-pre-handoff --force, mark-ready.
2. Alternativa (NO recomendada): mantener `mixed` y crear un commit en repo_motor que
   mencione 012a. Seria un commit vacio/artificial -> falso productivo. Rechazado por CEM.

## No declarar READY_FOR_REVIEW
No se fuerza mark-ready ni se inventa commit de codigo. Se entrega este CG con la
enmienda exacta. El trabajo queda staged y correcto en repo_destino (9640511), a la
espera de reclasificar el deliverable_type.
