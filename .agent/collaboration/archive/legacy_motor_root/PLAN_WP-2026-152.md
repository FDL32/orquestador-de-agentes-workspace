# Plan - WP-2026-152

## Goal
Repair the `--request-changes` handoff so the requeue does not deadlock after the bus has already advanced to `IN_PROGRESS` because of an immediate `REVIEW_DECISION=changes`. Preserve the bridge stderr logging for failed `--request-changes` calls, and keep supervisor dedupe changes out of scope.

## Strategy
1. Update `_handle_request_changes()` to derive `pending_requeue` from the `events` slice already read for bus-state derivation, using `events[-1]` when present, and branch explicitly: `UNKNOWN` fallback, `READY_FOR_REVIEW` normal path, `IN_PROGRESS` only with `pending_requeue`, everything else closed.
2. Keep the bridge stderr logging for non-zero `--request-changes` returncodes as an observability aid only.
3. Add direct tests for the allowed requeue path, the generic `IN_PROGRESS` rejection path, the `UNKNOWN` + execution_log fallback path, and the bridge logging path.
4. Leave supervisor dedupe/relaunch policy untouched for this ticket.

## Files Likely Touched
- `.agent/agent_controller.py`
- `bus/review_bridge.py`
- `tests/unit/test_request_changes_requeue.py`
- `tests/unit/test_review_bridge_request_changes_logging.py`

## Acceptance Shape
- The controller accepts a pending requeue only when the bus history proves an immediate `REVIEW_DECISION=changes` antecedent.
- Generic `IN_PROGRESS` without that antecedent still fails closed.
- The handler reuses the bus events already read in the function and does not perform a redundant `latest_event()` bus read.
- The existing `UNKNOWN` fallback to execution_log remains intact.
- The bridge logs the `--request-changes` failure returncode and stderr.
- Tests cover the allowed path, the blocked path, the `UNKNOWN` fallback path, and the logging behavior.
- No supervisor dedupe/trigger changes are included.
- Validation stays clean.
