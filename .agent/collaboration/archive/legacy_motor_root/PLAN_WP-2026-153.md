# Plan - WP-2026-153

## Goal
Create a new pre-plan grilling skill that forces one-question-at-a-time clarification before work-plan creation. The skill must use `PROJECT.md` and `MEMORY.md` as default context, allow `CONTEXT.md` only when useful, and end with an explicit handshake that signals it is safe to start the WP. Update the skills catalog and the repo notes so the workflow is discoverable.

## Strategy
1. Create `skills/grill-work-plan/SKILL.md` with frontmatter, explicit triggers (`/grill-plan`, `/grill`, `grill-wp`), the ordered workflow, optional `CONTEXT.md` behavior at project root, and ADR criteria.
2. Register the new skill in `skills/README.md`.
3. Update `README.md`, `PROJECT.md`, and `CHANGELOG.md` so the workflow is discoverable.
4. Keep any `man-create-work-plan` integration optional and defer it if it would add behavior to the hot path.

## Files Likely Touched
- `skills/grill-work-plan/SKILL.md`
- `skills/README.md`
- `README.md`
- `PROJECT.md`
- `CHANGELOG.md`

## Acceptance Shape
- The new skill exists and is discoverable from the skills catalog.
- The skill uses `PROJECT.md` + `MEMORY.md` by default and keeps `CONTEXT.md` optional.
- The skill ends with the exact handshake line `> ✅ Grill completo. Términos resueltos: N. Puedes crear el WP con /plan.`
- The documentation updates make the workflow discoverable without forcing code-path changes.
- Validation stays clean.
