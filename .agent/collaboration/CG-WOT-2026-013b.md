# CG-WOT-2026-013b -- CONTRACT_GAP

- **ticket_id:** WOT-2026-013b
- **emitido:** 2026-06-21
- **rol:** BUILDER
- **veredicto:** BLOCKED (pipeline detenido; 011i NO avanza)

## Resumen

El contrato T-013B-001 presume `documented_non_parallel_safe_count=3`: una familia
estable de exactamente 3 tests no parallel-safe a aislar en `tests/unit/`. La
reproduccion de Fase 0 NO confirma esa premisa. Se emite CONTRACT_GAP conforme al
propio `CONTRACT_GAP behavior` del contrato, su `STOP condition` ("parar si los
tests rojos actuales no son exactamente una familia de aislamiento de suite [de 3]")
y la regla de pipeline del usuario ("si no reaparece un set estable de 3 tests rojos
bajo xdist, bloquea con CG-WOT-2026-013b.md y deten la cadena").

## Evidencia de Fase 0 (read-only, sin diff productivo)

Comando: `uv run python scripts/run_pytest_safe.py --level unit --xdist-workers auto`
(con `uv run`; xdist 3.8.0 esta en deps via pyproject `pytest-xdist>=3.6` y solo se
resuelve bajo el entorno uv, no con el python del sistema).

- **Run 1:** 12 failed, 3068 passed, 20 skipped.
- **Run 2:** 11 failed, 3069 passed, 20 skipped.
- **Set NO estable:** diff run1un2 = `test_detect_version.py::TestVersionDetection::test_upgrade_path_suggestion`
  aparece en run1 y desaparece en run2 (flaky). Los otros 11 persisten.
- **Composicion != 3, != familia unica:**
  - 11/12 en `tests/unit/test_project_root_resolution.py` (TestDerivedPaths x5,
    TestResolveProjectRoot x6).
  - 1/12 (flaky) en `tests/unit/test_detect_version.py`.

## Naturaleza del rojo (por que NO es "aislar 3 tests")

- Firma dominante: `ImportError: module runtime.project_root not in sys.modules`
  al ejecutar `importlib.reload()` dentro de los tests de project_root.
- Un test representativo (`test_get_agent_dir`) **pasa aislado e incluso con `-n 2`**;
  solo falla en el run completo con 8 workers. Es contencion de `sys.modules`/estado
  global de modulo entre workers segun el reparto de carga, NO un defecto intrinseco
  de 3 tests concretos.
- El numero y los nombres cambian con el numero de workers y el orden de reparto:
  no hay un "set de 3" determinista que tachar.

## Por que bloquea (limites del contrato)

- La via verde NO se puede acotar a `tests/unit/` tachando 3 nombres: requeriria
  rearquitectura del aislamiento de modulos/`sys.modules` (p.ej. evitar
  `importlib.reload` global, o forzar `--dist loadscope`/agrupar por archivo en el
  runner). Ambas cosas caen en **Forbidden Surfaces** del contrato
  (`scripts/run_pytest_safe.py`, semantica de `--level unit`, codigo productivo).
- `Integracion cross-ticket` del contrato: "si el rojo apunta al runner o a producto,
  ese follow-up pertenece a otra familia y 013b debe bloquear".

## Recomendacion (no ejecutada; decision del humano/Manager)

Reescribir 013b como dos piezas y re-congelar contrato antes de relanzar Builder:
1. **Aislamiento de `test_project_root_resolution.py`** (familia real): hacer sus
   tests parallel-safe sin tocar el runner (fixtures que no dependan de
   `importlib.reload` de un modulo global compartido, o marcarlos
   `@pytest.mark`+`--dist loadscope` si se decide tocar runner -> otra familia).
2. **Politica de distribucion del runner** (`--dist loadscope` por defecto para unit)
   -> pertenece a la familia del runner (011e/011i), no a 013b.
Hasta entonces, **011i queda bloqueado** (su premisa "subset unit verde bajo xdist"
no se sostiene).

## Estado

- Sin cambios productivos. `tests/unit/` intacto. Runner/CI/controller intactos.
- Pipeline detenido en 013b por orden explicita del usuario. 011i NO se inicia.
