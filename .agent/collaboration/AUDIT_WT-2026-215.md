# AUDIT_WT-2026-215

## Tipo
Tooling de review/gates (review_bridge, prepush_check, session_closeout). Tier 3.

## Evidencia minima requerida
- Diff productivo revisable en `repo_motor` con commit `WT-2026-215`.
- `tests/test_motor_root_gates.py` presente y pasando.
- Test de regresion que falla sin el fix mediante revert parcial seguro.
- Decision sobre `_get_untracked_files` registrada por Manager en
  `execution_log.md` antes del handoff.
- `ruff` limpio en los 3 archivos principales.
- `validate --json` con 0 errores y 0 warnings.

## TP Check
TP-01: existe `_motor_root_or_raise()` (o equivalente) en `review_bridge.py`; no hay
  ramas paralelas de resolucion de motor_root en las funciones de evidencia.
TP-02: `check_review_packet_diff_empty` devuelve `False` con motor que tiene commits
  reales y workspace sin commits de codigo.
TP-03: decision sobre `_get_untracked_files` (L1096) documentada por Manager en
  `execution_log.md` antes del handoff; test cubre el caso elegido.
TP-04: `subprocess.run` en `_git_diff_stat`, `_build_diff_for_files_likely_touched`,
  `_resolve_review_base` y derivadas usa `cwd=motor_root`.
TP-05: `prepush_check.py` y `session_closeout.py` usan `cwd=motor_root` en sus checks
  de git; fallback sin crash si no hay link.
TP-06: call sites fuera de scope (repomix L483, review transport L1907/2011/3222/3280,
  `_run_script`) conservan `cwd=self.project_root` sin cambios.
TP-07: test de regresion demuestra fallo mediante revert parcial seguro del seam central.
TP-08: `ruff` limpio; `validate --json` 0/0.

## Blockers esperados
- CRITICO: reemplazar todos los `cwd=self.project_root` sin clasificar (cambia call sites
  fuera de scope; puede romper repomix y review transport).
- CRITICO: mockear `subprocess.run` o `git` en vez de usar repos git reales en los tests
  (el fix requiere que `cwd` correcto llegue a `subprocess`).
- CRITICO: `_motor_root_or_raise()` que hace crash silencioso o devuelve `project_root`
  como fallback (oculta el bug en vez de hacerlo visible).
- ALTO: crear un test nuevo con nombre ligado al ticket (`test_wt_*` / `test_wp_*`)
  en vez de nombre funcional; la trazabilidad del ticket debe quedar en docstring y commit.
- ALTO: test de regresion que no demuestra fallo sin el fix.
- ALTO: Builder intenta escribir `.agent/collaboration/` o la decision Manager sobre
  `_get_untracked_files` no esta registrada antes del handoff.
- MEDIO: cambios fuera de `Files Likely Touched` sin justificacion CEM.
- MEDIO: fallback en prepush/closeout que bloquea cuando el link no existe
  (debe ser warning, no bloqueante, en esos contextos).

## Revision Manager
El Manager debe verificar mecanicamente:
- `git -C C:\Users\fdl\Proyectos_Python\orquestador_de_agentes log --oneline -3`
  contiene commit con `WT-2026-215`.
- `git show --stat <commit>` toca `bus/review_bridge.py`, `scripts/prepush_check.py`
  y/o `scripts/session_closeout.py`, y los tests.
- `python -m pytest tests/test_motor_root_gates.py -v` -> todos pasan.
- `python -m pytest tests/test_manager_review_bridge.py -q` -> sin regresiones.
- `ruff check bus/review_bridge.py scripts/prepush_check.py scripts/session_closeout.py`
  -> All checks passed.
- Decision sobre `_get_untracked_files` presente en `execution_log.md`.
- Call sites fuera de scope sin cambios (grep `cwd=self.project_root` en repomix y
  review transport devuelve las mismas lineas que antes).
- `python ../orquestador_de_agentes/.agent/agent_controller.py --validate --json --project-root .`
  -> 0/0.
- El test de regresion falla con revert parcial seguro del seam central.
